export const metadata = {
  title: "Mon Dashboard — CVAdapt",
  description: "Retrouve tous tes CV générés, tes scores ATS et l'historique de tes candidatures.",
  robots: { index: false, follow: false },
};

export default function DashboardLayout({ children }) {
  return children;
}
